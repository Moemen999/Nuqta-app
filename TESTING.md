# الاختبارات في نقطة

## الأوامر

| الأمر | بيعمل إيه | محتاج إيه |
|---|---|---|
| `npm test` | اختبارات المنطق، مرة عادي وبعدين في 4 توقيتات مختلفة | مفيش |
| `npm run test:fast` | نفس الاختبارات مرة واحدة بس (أسرع وانت بتشتغل) | مفيش |
| `npm run test:watch` | وضع المراقبة | مفيش |
| `npm run test:tz` | اختبارات الحسابات في 4 توقيتات (القاهرة/UTC/نيويورك/كاتماندو) | مفيش |
| `npm run test:db` | اختبارات الكتابة في قاعدة البيانات على محاكي Firestore | Java |

## ليه التوقيتات؟

حسابات التاريخ اتكسرت قبل كده بسبب الخلط بين UTC والتوقيت المحلي، والباجات دي
مكانتش بتظهر خالص في UTC (اللي بيشتغل عليه الـ CI عادةً) — كانت بتظهر عند
المستخدم المصري بس. عشان كده `scripts/test-timezones.js` بيشغّل جيست من الأول
بتوقيت مختلف كل مرة. تغيير `process.env.TZ` من جوه الاختبار **مبيأثرش** لأن جيست
بيدي كل ملف نسخة معزولة من `process`، فالطريقة دي هي الوحيدة الصادقة.

## قواعد Firestore — مهم

**قواعد الأمان بتتنشر من الريبو، مش من الكونسول**:

```bash
firebase deploy --only firestore:rules
```

- `firestore.rules` → القواعد الحقيقية بتاعة الإنتاج، متتبعة في Git ودي اللي
  بتتنشر (`firebase.json` بيشاور عليها). أي تعديل عليها يبقى في PR زي أي كود.
- `firestore.emulator.rules` → مفتوحة عن قصد، مش مستخدمة دلوقتي في أي حاجة —
  متسابة موجودة لو احتجنا اختبار منطق كتابة بحت من غير الاعتماد على مستخدم
  حقيقي مسجّل دخول. **متتنشرش أبدًا.**
- اختبارات المحاكي (`npm run test:db`) بقت بتشغّل **قواعد الإنتاج نفسها**
  (`firestore.rules`) عن طريق `firebase.emulator.json`، مع محاكي Auth حقيقي —
  مش قواعد مفتوحة. فلو حد ضاف حقل جديد في `DataContext.tsx` من غير ما يحدّث
  القواعد، الاختبارات هتفشل بـ `permission-denied` بدل ما الباج يتكشف بعد
  النشر بس.

## اختبارات قاعدة البيانات

`npm run test:db` بيقوم محاكي Firestore ومحاكي Auth المحليين
(`firebase emulators:exec --only firestore,auth`)، وبيحط
`FIRESTORE_EMULATOR_HOST` و`FIREBASE_AUTH_EMULATOR_HOST` تلقائيًا، و
`firebaseConfig.js` بيوصّل عليهم بناءً على المتغيرين دول. **الاختبارات دي
مبتلمسش قاعدة البيانات الحقيقية ولا مشروع Auth حقيقي خالص** — من غير المتغيرين
دول الكود مبيمرش من السطرين دول أصلاً. محتاج Java عشان المحاكيات شغالة عليها.

- بتشغّل `DataProvider` الحقيقي بكل الـ `onSnapshot` بتوعه (مش نسخة مقلّدة)،
  عشان تعدّي على نفس الكود اللي التطبيق بيستخدمه
- `AuthContext` مقلّد عشان نتحكم في الـ uid اللي `DataProvider` بيستخدمه في
  مسارات Firestore، لكن `test-utils/emulator.ts` بتاعة `signInTestUser()`
  بتسجّل دخول **مستخدم حقيقي** (anonymous) على محاكي Auth وبترجع الـ uid
  الحقيقي بتاعه — ده اللي بنمرره لـ `setMockUid`. من غير كده قواعد الإنتاج
  هترفض كل كتابة لأن `request.auth` هيبقى فاضي
- بتستخدم `firestore.rules` (قواعد الإنتاج الحقيقية) عن طريق
  `firebase.emulator.json` — مش قواعد مفتوحة
- بتشتغل ملف ورا ملف (`maxWorkers: 1`) لأن كلهم على نفس المحاكي
- `context/__tests__/sync.emulator.test.tsx` بتقفل النت فعلاً على المحاكي
  (`disableNetwork`) وبتتأكد إن الحفظ بيرجع على طول من غير ما يستنى السيرفر، وإن
  علامة "لسه بترفع" بتشتغل وبتختفي لوحدها لما النت يرجع. **متزوّدش قفل/فتح للنت
  أكتر من كده**: فايربيز بتسيب مجاري اتصال شغالة بعد التوجلة دي، وأخطاؤها
  الجوانية بتقع على أي اختبار شغال ساعتها — حتى في ملف تاني — فالسويت كلها
  بتبقى مزاجية
- `context/__tests__/security.emulator.test.tsx` بتختبر القواعد نفسها مباشرة
  (من غير `DataProvider`): عزل بيانات المستخدمين عن بعض (قراءة/كتابة/مسح
  بيانات مستخدم تاني ترفض)، والتحقق من شكل البيانات (مبلغ سالب، نوع عملية غلط،
  اسم فاضي كلهم بيترفضوا)

## ملحوظة على الكتابات الذرية

`markGamiyaMonthDone` و`markSubscriptionPaid` و`claimSeeding` بيستخدموا
`runTransaction` عشان يقروا من السيرفر ويتأكدوا إن العملية ما اتعملتش قبل كده.
لما اختبارات التزاحم تشتغل هتشوف في اللوج تحذيرات `failed-precondition` أو
`already-exists` — **دي مش أخطاء**، دي بالظبط النداء التاني وهو بيترفض ويعيد
المحاولة ويلاقي الشغل اتعمل خلاص.

**`runTransaction` مبتفضلش تحاول للأبد**: اتقاست فعليًا على عميل بيكلم بورت
مقفول — بترفض بـ `unavailable` بعد **حوالي 10 ثواني** (5 محاولات بـ backoff،
والمصدر: `TransactionRunner` + `isPermanentError`). يعني لو الاتصال وقع في نص
تسديد، العملية بتموت بعد المهلة دي ومش هتكمل لوحدها لما النت يرجع. عشان كده
الزرار بيستنى الإجابة دي وهو بيلف، وبعدين بيقول للمستخدم كلام قاطع: اتسجلت، ولا
مفيش أي خصم اتسجل (العملية الذرية إما تتم كلها أو مفيش، فالإعادة آمنة).
وبالمقابل `waitForPendingWrites` **مبيتحلش خالص** وانت أوفلاين (اتقاس كمان) —
عشان كده هو متلفوف بسقف زمني + إشارة "الاتصال وقع" في `waitForOurWritesToLand`.

**الاختبارات لازم تستنى `waitForReady()`** مش وصول المحافظ بس: المحافظ
الافتراضية بتظهر من الكاش المحلي قبل ما السيرفر يأكدها، وفي اللحظة دي التطبيق
لسه شايف نفسه مش متصل، فأي تسديد بيترفض بـ `no-connection`.

أي عملية جديدة بتكتب فلوس لازم تمشي بنفس النمط ده — قفل الزرار في الواجهة
بيغطي الضغط السريع بس ومش حماية على مستوى البيانات.
